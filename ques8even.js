num = Math.floor(Math.random() * (100 - 1)) + 1;   //will generate random no between highest and lowest no
console.log(`Random value between 1 and 100 is ${num}`);
if(num % 2 == 0)
        console.log("Even");
    else
        console.log("Odd");
    
